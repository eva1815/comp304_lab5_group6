<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    style="@style/AppTheme"
    tools:context=".LoginActivity">


    <ImageView
        android:id="@+id/image_college_logo"
        android:layout_width="300dp"
        android:layout_height="200dp"
        android:src="@drawable/college_logo"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/textview_log_in"
        android:textColor="@color/black"
        android:textSize="30sp"/>

    <EditText
        android:layout_marginTop="10dp"
        android:id="@+id/et_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/edittext_enter_email"
        android:inputType="textEmailAddress"/>

    <EditText
        android:layout_marginTop="10dp"
        android:id="@+id/et_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/editText_enter_password"
        android:inputType="textPassword"/>

    <Button
        android:layout_marginTop="20dp"
        android:id="@+id/btn_login"
        android:layout_width="180dp"
        android:layout_height="50dp"
        android:textSize="20dp"
        android:textColor="@color/white"
        android:background="@drawable/login_btn"
        android:text="@string/btn_log_in"/>

    <TextView
        android:layout_marginTop="20dp"
        android:id="@+id/tv_signup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="18dp"
        android:textColor="@color/stop_button_red"
        android:text="@string/textView_not_an_employee_sign_up_here"/>

</LinearLayout>